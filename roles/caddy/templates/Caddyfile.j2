# Caddy - Development Environment
# https://caddyserver.com/docs/caddyfile
#
# Add your local sites below.
#
# Example (reverse proxy with automatic HTTPS):
#   app.localhost {
#       reverse_proxy localhost:3000
#   }
#
# Example (serve static files):
#   files.localhost {
#       root * /path/to/files
#       file_server
#   }
